services:
  app:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
    env_file:
      - prototype/.env
    volumes:
      - ./prototype/serviceAccountKey.json:/app/prototype/serviceAccountKey.json:ro
